# Image Prompts — Sesión 04: JSON + Parse JSON

> Esta infografía no requiere imágenes generadas con IA. Todos los conceptos se representan mediante HTML/CSS (cards con qué es JSON, comparación texto libre vs JSON, el Data Structure y el flujo Gmail → Gemini → Parse JSON → Sheet).
